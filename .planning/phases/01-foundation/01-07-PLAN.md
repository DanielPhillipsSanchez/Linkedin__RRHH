---
phase: 01-foundation
plan: 07
type: execute
wave: 3
depends_on:
  - 01-04
  - 01-05
  - 01-06
files_modified: []
autonomous: false
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-04
  - SET-05
  - SET-06

must_haves:
  truths:
    - "Extension loads in Chrome developer mode without console errors"
    - "Options page opens in a full tab when the recruiter clicks through from the popup"
    - "Recruiter can enter, save, and validate their API key — key input clears after save"
    - "Recruiter can create a JD with a title and raw text"
    - "Recruiter can add skills to a JD and toggle mandatory/nice-to-have"
    - "Recruiter can select an active JD and verify it persists after page reload"
    - "Recruiter can delete a JD and verify the active selection clears if it was the deleted JD"
    - "API key is not visible in Chrome DevTools Network tab (validation request goes to background)"
  artifacts:
    - path: ".output/chrome-mv3/"
      provides: "Loadable Chrome extension"
    - path: ".output/safari-mv3/"
      provides: "Safari extension build output"
  key_links:
    - from: "Chrome extension popup"
      to: "Options page tab"
      via: "chrome.runtime.openOptionsPage()"
    - from: "Options page API key section"
      to: "Background service worker"
      via: "browser.runtime.sendMessage VALIDATE_API_KEY"
---

<objective>
Human verification of all Phase 1 features in a real Chrome browser. This checkpoint confirms the extension works end-to-end as a recruiter would use it, not just in unit tests.

Purpose: Unit tests verify logic; only a real browser confirms the extension loads, the popup opens, the options page renders, and all interactions work. This is the phase gate before Phase 2 begins.
Output: Signed-off Phase 1 with all SET-0x requirements confirmed working.
</objective>

<execution_context>
@/Users/danielphillips/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielphillips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-05-SUMMARY.md
@.planning/phases/01-foundation/01-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify Phase 1 foundation in Chrome browser</name>
  <files>none — human verification only</files>
  <action>Human reviews the built extension in Chrome developer mode following the verification steps below.</action>
  <what-built>
    Complete Phase 1 foundation:
    - WXT Chrome MV3 extension (Plans 01-06)
    - Options page with API key entry, JD management, skill weighting, active JD selection
    - Background service worker with VALIDATE_API_KEY handler
    - Safari Xcode project scaffold
    - All unit tests passing (pnpm vitest run exits 0)
  </what-built>
  <how-to-verify>
    Run `pnpm wxt build` from the project directory to ensure a clean build, then:

    **Step 1 — Load in Chrome:**
    1. Open Chrome → `chrome://extensions`
    2. Enable "Developer mode" (top right toggle)
    3. Click "Load unpacked" → select `.output/chrome-mv3/`
    4. Confirm: Extension appears in the list with name "LinkedIn HHRH Screener", no error badge

    **Step 2 — Popup:**
    5. Click the extension icon in the toolbar
    6. Confirm: Popup opens showing a "Settings →" link or similar
    7. Click the settings link
    8. Confirm: Options page opens in a new full tab (not in the chrome://extensions panel)

    **Step 3 — API Key (SET-01/02):**
    9. In the options page, enter any text in the API key field (e.g., your real `sk-ant-...` key or a test string like `sk-ant-test`)
    10. Click "Save &amp; Validate"
    11. Confirm: Status shows "Validating..." then either "API key saved and validated" or a specific error
    12. Confirm: The key input field is cleared after clicking save
    13. Confirm: "A key is saved" indicator appears below the input
    14. Open Chrome DevTools → Network tab → confirm the validation request goes to `api.anthropic.com` from the background context, NOT directly from the options page

    **Step 4 — JD Management (SET-03/04):**
    15. Create a new JD: enter a title (e.g., "Senior Engineer") and paste some text into the textarea
    16. Click "Add Job Description"
    17. Confirm: JD appears in the list with its title
    18. Create a second JD with a different title
    19. Confirm: Both JDs appear in the list
    20. Delete one JD using its Delete button
    21. Confirm: Only the remaining JD shows in the list

    **Step 5 — Skill Weighting (SET-05):**
    22. Expand the skills editor for the remaining JD (click "Edit Skills")
    23. Add a skill "TypeScript" with weight "Mandatory"
    24. Add a skill "Docker" with weight "Nice-to-have"
    25. Confirm: Both skills appear with their correct weight radio buttons checked
    26. Reload the options page (Cmd+R or close and reopen)
    27. Confirm: Both skills and their weights persisted

    **Step 6 — Active JD Selection (SET-06):**
    28. In the "Active Job Description" section, select the JD you created
    29. Reload the options page
    30. Confirm: The same JD is still selected after reload
    31. Delete the active JD
    32. Reload
    33. Confirm: No JD is selected (radio group shows unselected)

    **Step 7 — Console check:**
    34. Open Chrome DevTools → Console tab on the options page
    35. Confirm: No red errors in the console
    36. Open background service worker DevTools: chrome://extensions → click "Service Worker" link → Console tab
    37. Confirm: No red errors, "[HHRH] Background service worker initialized" log visible
  </how-to-verify>
  <resume-signal>
    Type "approved" if all steps pass.
    Or describe any issues (e.g., "Step 11 fails — status never shows, stays blank") so they can be diagnosed and fixed before Phase 2.
  </resume-signal>
  <verify>
    <automated>MISSING — this is a human-verify checkpoint; automated verification not applicable</automated>
  </verify>
  <done>Human types "approved" after completing all 7 verification steps without finding blocking issues.</done>
</task>

</tasks>

<verification>
Human verification is the verification for this plan. All 7 steps above must pass.
</verification>

<success_criteria>
Recruiter (you) can load the extension in Chrome, manage job descriptions with skill weighting, save and validate an API key, select an active JD, and confirm all state persists across page reloads — no console errors. Phase 1 is complete.
</success_criteria>

<output>
After approval, create `.planning/phases/01-foundation/01-07-SUMMARY.md` documenting:
- Which steps passed and which (if any) needed fixes before approval
- Any bugs discovered and fixed during verification
- Final confirmation that all SET-01 through SET-06 requirements are satisfied
- Note any concerns or observations to carry into Phase 2
</output>
