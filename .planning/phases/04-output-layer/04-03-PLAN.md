---
phase: 04-output-layer
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: []
autonomous: false
requirements: [MSG-01, MSG-02, MSG-03, MSG-04, MSG-05, MSG-06, CSV-01, CSV-02, CSV-03, CSV-04, CSV-05]

must_haves:
  truths:
    - "Recruiter evaluates a real LinkedIn profile and receives a tier + score in the popup"
    - "Recruiter clicks 'Generate Message' and receives tier-appropriate outreach text in the textarea"
    - "Recruiter edits the textarea content successfully"
    - "Recruiter clicks 'Open LinkedIn Message' and a new tab opens to a LinkedIn compose page"
    - "Recruiter clicks 'Mark as Sent' and the UI confirms the action"
    - "Recruiter clicks 'Export CSV' and a CSV file downloads with the evaluated candidate's data"
    - "CSV file opened in a spreadsheet has 10 columns: Name, Title, LinkedIn URL, Tier, Match Score (%), Matched Skills, Missing Skills, Evaluation Date, Contact After, Outreach Message Sent"
    - "The sent message text appears in the 'Outreach Message Sent' column after Mark as Sent"
  artifacts:
    - path: "linkedin-hhrh-screener/entrypoints/popup/index.html"
      provides: "All output-layer UI elements present and functional in Chrome"
      contains: "message-textarea"
  key_links:
    - from: "popup (human test)"
      to: "LinkedIn compose window"
      via: "open-linkedin-btn -> browser.tabs.create"
      pattern: "manual verification — compose tab opens"
---

<objective>
Human verification checkpoint: the recruiter (you) walks through the complete output-layer workflow in Chrome to confirm that message generation, editing, LinkedIn compose, mark-as-sent, and CSV export all work end-to-end.

Purpose: Unit tests confirm handler logic in isolation. This checkpoint confirms the popup wiring, the LinkedIn compose URL behavior, the CSV file content, and the full recruiter workflow feel correct in a real browser session. Many of these behaviors (DOM interaction, file download, external URL) cannot be automated.

Output: Human confirmation that Phase 4 is complete, or a list of issues for the executor to fix before sign-off.
</objective>

<execution_context>
@/Users/danielphillips/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielphillips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-output-layer/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build extension and confirm full test suite passes</name>
  <files></files>
  <action>
    Run the full vitest suite and confirm it is green before asking the recruiter to test:
    ```
    cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx vitest run
    ```

    Then build the extension for Chrome:
    ```
    cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx wxt build
    ```

    If the build fails, diagnose and fix the error. Common causes:
    - TypeScript type errors introduced during Wave 1 fixes
    - Missing imports in popup/index.ts
    - WXT entrypoint configuration issues

    After a successful build, confirm the output directory exists:
    ```
    ls /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener/.output/chrome-mv3/
    ```

    Do NOT proceed to the human checkpoint until both the test suite and the build pass.
  </action>
  <verify>
    <automated>cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx vitest run && npx wxt build</automated>
  </verify>
  <done>vitest run produces 0 failures. wxt build completes with no errors. .output/chrome-mv3/ directory exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full output-layer recruiter workflow in Chrome</name>
  <what-built>
    Complete output layer: AI message generation, message editing, LinkedIn compose window opener, mark-as-sent persistence, and CSV export. All implemented across background.ts, popup/index.ts, popup/index.html, src/scorer/messenger.ts, and src/shared/csv.ts.
  </what-built>
  <how-to-verify>
    Prerequisites:
    - Snowflake/Cortex credentials configured in the extension Options page
    - An active Job Description selected in Options
    - Chrome with the extension loaded from .output/chrome-mv3/

    Step 1 — Evaluate a candidate (confirms Phase 3 still works):
    1. Open any LinkedIn profile page (e.g. someone's /in/ page)
    2. Click the extension icon to open the popup
    3. Click "Evaluate"
    4. Confirm the result section shows: tier label, match score %, matched skills, missing skills, rationale
    5. Confirm the popup does NOT show a message textarea yet (message section should be hidden)

    Step 2 — Generate outreach message (MSG-01/02/03):
    1. Click "Generate Message" in the popup
    2. Wait for the Cortex API call (may take 2-5 seconds)
    3. Confirm a message appears in the textarea
    4. Confirm the tone feels appropriate for the tier:
       - L1: direct, enthusiastic ("I'd love to connect about...")
       - L2: exploratory ("I came across your profile and wanted to reach out...")
       - L3: future-opportunity framing ("We'll be in touch in the coming weeks...")
    5. If message generation fails, note the error text shown in the popup

    Step 3 — Edit the message (MSG-04):
    1. Click into the textarea and change some text
    2. Confirm the textarea is editable (not readonly)

    Step 4 — Open LinkedIn compose window (MSG-05):
    1. Click "Open LinkedIn Message"
    2. Confirm a new tab opens to LinkedIn's messaging compose page
    3. Check the "To:" field: is the candidate's name pre-filled?
       - If yes: fully working
       - If the compose window opens but "To:" is empty: acceptable for v1 — note this outcome
       - If the tab opens to a non-compose page or 404: this is a bug — note the URL that opened

    Step 5 — Mark as sent (MSG-06):
    1. (In the extension popup) click "Mark as Sent"
    2. Confirm the UI shows a confirmation (button changes text, or a status message appears)

    Step 6 — Export CSV (CSV-01 through CSV-05):
    1. Click "Export CSV" in the popup
    2. Confirm a CSV file downloads (browser download bar or Downloads folder)
    3. Open the file in a spreadsheet application (Numbers, Excel, or Google Sheets)
    4. Confirm 10 columns with headers: Name, Title, LinkedIn URL, Tier, Match Score (%), Matched Skills, Missing Skills, Evaluation Date, Contact After, Outreach Message Sent
    5. Confirm the evaluated candidate appears as a row with correct data
    6. If you clicked "Mark as Sent" in Step 5: confirm the "Outreach Message Sent" column contains the message text
    7. If the candidate is L3: confirm "Contact After" shows a date 7 days after evaluation
    8. If the candidate is L1 or L2: confirm "Contact After" is empty

    What to report:
    - Type "approved" if all 6 steps pass (LinkedIn compose pre-fill may be partial — that is acceptable)
    - Or describe any issues found (which step, what was expected, what actually happened)
  </how-to-verify>
  <action>Human recruiter tests the full output-layer workflow in Chrome following the steps in how-to-verify. No automated action — this is a human checkpoint.</action>
  <verify>
    <automated>MISSING — this is a checkpoint:human-verify task; automated verification is not applicable</automated>
  </verify>
  <done>Recruiter types "approved" confirming all six verification steps pass, or issues are documented for the executor to address.</done>
  <resume-signal>Type "approved" to complete Phase 4, or describe issues for the executor to fix</resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when:
1. Full vitest suite passes (all tests green, including the new background.test.ts and csv.test.ts additions from Plan 01)
2. wxt build succeeds with no TypeScript errors
3. Human recruiter workflow completes Steps 1-6 above without blocking issues
</verification>

<success_criteria>
- vitest run: 0 failures across messenger.test.ts, background.test.ts, csv.test.ts, and all other test files
- wxt build: succeeds with Chrome MV3 output
- Human verification: MSG-01 through MSG-06 and CSV-01 through CSV-05 confirmed working in Chrome
- LinkedIn compose window behavior documented (full pre-fill, partial pre-fill, or empty — all noted)
- CSV downloaded and verified to contain all 10 required columns
</success_criteria>

<output>
After human approval, create `.planning/phases/04-output-layer/04-03-SUMMARY.md`

Update .planning/ROADMAP.md: Phase 4 status to Complete with today's date.
Update .planning/STATE.md: current phase to Phase 5.
</output>
