---
phase: 04-output-layer
plan: 04
type: execute
wave: 4
depends_on: [04-02]
files_modified:
  - linkedin-hhrh-screener/src/storage/schema.ts
  - linkedin-hhrh-screener/src/storage/storage.ts
  - linkedin-hhrh-screener/src/scorer/claude.ts
  - linkedin-hhrh-screener/src/scorer/messenger.ts
  - linkedin-hhrh-screener/entrypoints/background.ts
  - linkedin-hhrh-screener/entrypoints/options/index.ts
  - linkedin-hhrh-screener/entrypoints/options/index.html
  - linkedin-hhrh-screener/wxt.config.ts
autonomous: true
gap_closure: true
requirements: [MSG-01, MSG-02, MSG-03, MSG-04, MSG-05, MSG-06, CSV-01, CSV-02, CSV-03, CSV-04, CSV-05]

must_haves:
  truths:
    - "No runtime import of cortex.ts exists in background.ts, claude.ts, or messenger.ts"
    - "background.ts calls getAnthropicApiKey() (not getSnowflakeCredentials()) everywhere credentials are needed"
    - "validateStoredCredentials calls validateAnthropicApiKey from anthropic.ts (not validateCortexCredentials)"
    - "refineWithClaude(apiKey, profile, unmatchedSkills) calls anthropicComplete — cortexComplete is unreachable"
    - "generateOutreachMessage(apiKey, ...) takes a string API key (not CortexCredentials)"
    - "Options page shows only the Anthropic API key section — Snowflake fields do not exist in the DOM"
    - "wxt.config.ts host_permissions has api.anthropic.com but not snowflakecomputing.com"
  artifacts:
    - path: "linkedin-hhrh-screener/src/storage/schema.ts"
      provides: "Storage keys without SF_* entries; ANTHROPIC_API_KEY key"
      contains: "ANTHROPIC_API_KEY"
    - path: "linkedin-hhrh-screener/src/storage/storage.ts"
      provides: "saveAnthropicApiKey / getAnthropicApiKey helpers; no SnowflakeCredentials type"
      exports: ["saveAnthropicApiKey", "getAnthropicApiKey"]
    - path: "linkedin-hhrh-screener/src/scorer/claude.ts"
      provides: "refineWithClaude(apiKey: string, ...) using anthropicComplete"
      contains: "anthropicComplete"
    - path: "linkedin-hhrh-screener/src/scorer/messenger.ts"
      provides: "generateOutreachMessage(apiKey: string, ...) using anthropicComplete"
      contains: "anthropicComplete"
    - path: "linkedin-hhrh-screener/entrypoints/background.ts"
      provides: "All handlers use getAnthropicApiKey(); VALIDATE_API_KEY calls validateAnthropicApiKey"
      contains: "getAnthropicApiKey"
  key_links:
    - from: "entrypoints/background.ts"
      to: "src/storage/storage.ts"
      via: "getAnthropicApiKey import"
      pattern: "getAnthropicApiKey"
    - from: "entrypoints/background.ts"
      to: "src/scorer/anthropic.ts"
      via: "validateAnthropicApiKey import"
      pattern: "validateAnthropicApiKey"
    - from: "src/scorer/claude.ts"
      to: "src/scorer/anthropic.ts"
      via: "anthropicComplete import (no cortexComplete)"
      pattern: "anthropicComplete"
    - from: "src/scorer/messenger.ts"
      to: "src/scorer/anthropic.ts"
      via: "anthropicComplete import (no cortexComplete)"
      pattern: "anthropicComplete"
---

<objective>
Remove all Snowflake Cortex runtime usage and replace with direct Anthropic API calls across the entire codebase.

Purpose: Plan 04-02 committed a migration from Anthropic to Cortex that the recruiter wants reverted. This plan performs the clean reversal: schema/storage lose Snowflake keys; scorer and messenger switch back to anthropicComplete; background wires to the Anthropic API key; options shows only the API key field.

Output:
- Schema: `ANTHROPIC_API_KEY` is the only credential key; SF_* keys removed
- Storage: `saveAnthropicApiKey` / `getAnthropicApiKey` replace Snowflake helpers
- claude.ts: `refineWithClaude` calls `anthropicComplete` (Cortex path deleted)
- messenger.ts: `generateOutreachMessage` takes `apiKey: string`, calls `anthropicComplete`
- background.ts: All credential lookups use `getAnthropicApiKey()`; validation calls `validateAnthropicApiKey`
- options/index.html + index.ts: Snowflake section removed; single Anthropic API key section remains
- wxt.config.ts: Snowflake host permission removed
</objective>

<execution_context>
@/Users/danielphillips/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielphillips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-output-layer/04-02-SUMMARY.md

<interfaces>
<!-- Key contracts. Read the actual source files before editing — these are a structural guide only. -->

From linkedin-hhrh-screener/src/scorer/anthropic.ts (DO NOT MODIFY — this file is already correct):
```typescript
export async function anthropicComplete(
  apiKey: string,
  prompt: string,
  model?: string,
): Promise<AnthropicResponse>  // { text: string; error?: string }

export async function validateAnthropicApiKey(
  apiKey: string,
): Promise<{ valid: boolean; error?: string }>
```

Current storage.ts exports to KEEP (rename, not delete):
- saveClaudeApiKey(key) → rename to saveAnthropicApiKey(key)
- getClaudeApiKey() → rename to getAnthropicApiKey()

Current background.ts message handlers to UPDATE (not rewrite):
- VALIDATE_API_KEY → must call validateAnthropicApiKey(apiKey) instead of validateCortexCredentials(creds)
- handleEvaluate() → replace getSnowflakeCredentials() with getAnthropicApiKey(); update error message
- handleGenerateMessage() → replace getSnowflakeCredentials() with getAnthropicApiKey(); update error message

Current background.ts also handles VALIDATE_CLAUDE_API_KEY from options page.
After migration: options page will send VALIDATE_API_KEY (not VALIDATE_CLAUDE_API_KEY).
Remove the VALIDATE_CLAUDE_API_KEY handler from background.ts and consolidate to VALIDATE_API_KEY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update schema.ts and storage.ts — replace Snowflake with Anthropic</name>
  <files>
    linkedin-hhrh-screener/src/storage/schema.ts
    linkedin-hhrh-screener/src/storage/storage.ts
  </files>
  <action>
    Read both files in full before editing.

    **schema.ts changes:**
    1. In `STORAGE_KEYS`, remove `SF_ACCOUNT_URL`, `SF_PAT_TOKEN`, `SF_WAREHOUSE` keys
    2. Rename `CLAUDE_API_KEY: 'settings:claudeApiKey'` to `ANTHROPIC_API_KEY: 'settings:anthropicApiKey'`
    3. Remove the `Settings` interface entirely (it only contained Snowflake + activeJdId fields; the interface is not used elsewhere — verify with a search before deleting, and only delete if no imports reference `Settings`)
    4. Leave `CandidateRecord`, `JobDescription`, `Skill` interfaces untouched

    **storage.ts changes:**
    1. Remove the `SnowflakeCredentials` interface export
    2. Remove `saveSnowflakeCredentials()` and `getSnowflakeCredentials()` functions entirely
    3. Rename `saveClaudeApiKey` → `saveAnthropicApiKey` (same body, just `STORAGE_KEYS.ANTHROPIC_API_KEY`)
    4. Rename `getClaudeApiKey` → `getAnthropicApiKey` (same body, just `STORAGE_KEYS.ANTHROPIC_API_KEY`)
    5. Remove the deprecated `saveApiKey()` and `getApiKey()` functions (keyed off `STORAGE_KEYS.API_KEY` which is also being removed)
    6. Remove the `API_KEY` entry from `STORAGE_KEYS` in schema.ts as well (it was already marked deprecated)

    Do NOT touch: `saveJd`, `getAllJds`, `deleteJd`, `setActiveJdId`, `getActiveJdId`, `getStorageUsageBytes`, `saveCandidate`, `getAllCandidates`, `getCandidate`, `deleteCandidate`.

    After edits, confirm: `grep -n 'Snowflake\|sfAccount\|sfPat\|sfWarehouse\|SF_ACCOUNT\|SF_PAT\|SF_WAREHOUSE\|saveSnowflake\|getSnowflake\|SnowflakeCred\|API_KEY.*deprecated' linkedin-hhrh-screener/src/storage/schema.ts linkedin-hhrh-screener/src/storage/storage.ts` returns no matches.
  </action>
  <verify>
    <automated>cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx tsc --noEmit 2>&1 | head -40</automated>
  </verify>
  <done>schema.ts has ANTHROPIC_API_KEY, no SF_* keys. storage.ts exports saveAnthropicApiKey and getAnthropicApiKey, no Snowflake types or functions. TypeScript compiles with no errors in these two files.</done>
</task>

<task type="auto">
  <name>Task 2: Update claude.ts, messenger.ts, background.ts, wxt.config.ts, and options UI</name>
  <files>
    linkedin-hhrh-screener/src/scorer/claude.ts
    linkedin-hhrh-screener/src/scorer/messenger.ts
    linkedin-hhrh-screener/entrypoints/background.ts
    linkedin-hhrh-screener/wxt.config.ts
    linkedin-hhrh-screener/entrypoints/options/index.ts
    linkedin-hhrh-screener/entrypoints/options/index.html
  </files>
  <action>
    Read all six files in full before editing.

    **claude.ts:**
    1. Remove the `import { cortexComplete, type CortexCredentials } from './cortex'` line
    2. Delete the existing `refineWithClaude(creds: CortexCredentials, ...)` function entirely
    3. Rename `refineWithAnthropicApi` → `refineWithClaude`, keeping the exact same body
    4. The renamed function signature becomes: `refineWithClaude(apiKey: string, profile: CandidateProfile, unmatchedSkills: Skill[])`
    5. The `import { anthropicComplete } from './anthropic'` line stays

    **messenger.ts:**
    1. Remove the `import { cortexComplete, type CortexCredentials } from './cortex'` line
    2. Add `import { anthropicComplete } from './anthropic'`
    3. Change `generateOutreachMessage` signature: first parameter changes from `creds: CortexCredentials` to `apiKey: string`
    4. Inside the function body, replace `await cortexComplete(creds, prompt)` with `await anthropicComplete(apiKey, prompt)`
    5. Update the empty-response error message from `'Cortex returned empty message'` to `'Empty response from Claude API'`

    **background.ts:**
    1. In the import from `../src/storage/storage`: replace `getSnowflakeCredentials` with `getAnthropicApiKey`; also remove `saveSnowflakeCredentials` if present
    2. Add `import { validateAnthropicApiKey } from '../src/scorer/anthropic'` (new import line)
    3. Remove the `import { validateCortexCredentials } from '../src/scorer/cortex'` line
    4. Rewrite `validateStoredCredentials`:
       ```typescript
       export async function validateStoredCredentials(): Promise<{ valid: boolean; error?: string }> {
         const apiKey = await getAnthropicApiKey();
         if (!apiKey) return { valid: false, error: 'No Anthropic API key stored' };
         return validateAnthropicApiKey(apiKey);
       }
       ```
    5. In `handleEvaluate`:
       - Replace `const creds = await getSnowflakeCredentials()` with `const apiKey = await getAnthropicApiKey()`
       - Replace `if (!creds)` guard with `if (!apiKey)`, and update error message to `'No Anthropic API key — please add it in Options'`
       - Replace `await refineWithClaude(creds, profile, unmatchedSkills)` with `await refineWithClaude(apiKey, profile, unmatchedSkills)`
       - Update any Snowflake-specific warning strings (e.g. `'Snowflake auth failed'` → `'Claude API auth failed'`; `'Snowflake unreachable'` → `'Claude API unreachable'`; `'Cortex error:'` → `'Claude API error:'`)
    6. In `handleGenerateMessage`:
       - Replace `const creds = await getSnowflakeCredentials()` with `const apiKey = await getAnthropicApiKey()`
       - Replace `if (!creds)` with `if (!apiKey)`, update error message to `'No Anthropic API key — please configure it in Options'`
       - Replace `await generateOutreachMessage(creds, profile, ...)` with `await generateOutreachMessage(apiKey, profile, ...)`
    7. In the message listener `defineBackground` callback:
       - Remove the `VALIDATE_CLAUDE_API_KEY` handler block if it exists
       - The `VALIDATE_API_KEY` handler already calls `validateStoredCredentials()` — leave it, it now routes to Anthropic

    **wxt.config.ts:**
    Remove `'https://*.snowflakecomputing.com/*'` from `host_permissions`. Keep `'https://api.anthropic.com/*'` and `'https://www.linkedin.com/*'`.

    **options/index.html:**
    Remove the entire `<section id="api-key-section">` block (the Snowflake Cortex Connection section with `sf-account-url-input`, `sf-warehouse-input`, `sf-pat-input`, `api-key-save-btn`, `api-key-status`, `api-key-indicator`).
    The `<section id="claude-api-key-section">` block must remain. Update its heading from "Developer Mode — Claude API Key" to "Claude API Key" and remove the sentence about "instead of Snowflake Cortex" and "takes priority over Snowflake credentials". Replace the description with: "Enter your Anthropic Claude API key. Required for candidate scoring and message generation."

    **options/index.ts:**
    1. Remove the import of `saveSnowflakeCredentials` from storage (and `getSnowflakeCredentials` if imported)
    2. Remove `STORAGE_KEYS.SF_ACCOUNT_URL`, `STORAGE_KEYS.SF_PAT_TOKEN`, `STORAGE_KEYS.SF_WAREHOUSE` references
    3. Remove `updateCredentialIndicator()` function entirely
    4. Remove `handleCredentialSave()` function entirely
    5. Rename `handleClaudeApiKeySave` → `handleApiKeySave`, `handleClaudeApiKeyClear` → `handleApiKeyClear` (or leave names — only correctness matters)
    6. In `handleClaudeApiKeySave` (or renamed version): change the sendMessage call from `{ type: 'VALIDATE_CLAUDE_API_KEY', apiKey: key }` to `{ type: 'VALIDATE_API_KEY' }` (background validates from storage, not from the message payload — per existing SET-02 pattern)
    7. Update `updateClaudeApiKeyIndicator()` to read from `getAnthropicApiKey()` (import this instead of `getClaudeApiKey`); update display text to just reference "Anthropic API key" (remove Snowflake priority mention)
    8. In DOMContentLoaded: remove the `saveBtn` / `api-key-save-btn` event listener wiring; remove the `updateCredentialIndicator()` call
    9. Update element ID references for Claude section buttons to match current HTML ids (`claude-api-key-save-btn`, `claude-api-key-clear-btn`) — these are already correct, just verify

    After all edits, run:
    ```
    cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx tsc --noEmit 2>&1 | head -60
    ```
    Fix any TypeScript errors before proceeding.
  </action>
  <verify>
    <automated>cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx tsc --noEmit 2>&1 | head -60</automated>
  </verify>
  <done>TypeScript compiles with 0 errors. No import of cortex.ts, SnowflakeCredentials, cortexComplete, validateCortexCredentials, or getSnowflakeCredentials remains in any file outside of cortex.ts itself. Options HTML has no Snowflake fields. wxt.config.ts has no snowflakecomputing.com permission.</done>
</task>

</tasks>

<verification>
After both tasks:
```
cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx tsc --noEmit && grep -rn 'cortexComplete\|getSnowflakeCredentials\|SnowflakeCredentials\|CortexCredentials\|validateCortexCredentials\|snowflakecomputing' src/ entrypoints/ wxt.config.ts 2>/dev/null
```

Expected: `tsc --noEmit` produces no errors. The grep returns no matches (cortex.ts itself is excluded — it is unused dead code but not harmful to leave).
</verification>

<success_criteria>
- TypeScript compiles with 0 errors across the project
- All Snowflake/Cortex runtime references removed from production code paths
- background.ts exclusively uses getAnthropicApiKey() + validateAnthropicApiKey()
- claude.ts refineWithClaude() calls anthropicComplete()
- messenger.ts generateOutreachMessage() takes apiKey: string, calls anthropicComplete()
- Options page shows only a single Anthropic API key section
- wxt.config.ts host_permissions contains only linkedin.com and api.anthropic.com
</success_criteria>

<output>
After completion, create `.planning/phases/04-output-layer/04-04-SUMMARY.md`
</output>
