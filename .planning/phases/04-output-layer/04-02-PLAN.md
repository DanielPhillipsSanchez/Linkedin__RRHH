---
phase: 04-output-layer
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - linkedin-hhrh-screener/src/scorer/messenger.ts
  - linkedin-hhrh-screener/entrypoints/popup/index.ts
  - linkedin-hhrh-screener/entrypoints/popup/index.html
  - linkedin-hhrh-screener/entrypoints/popup/style.css
  - linkedin-hhrh-screener/src/shared/csv.ts
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03, MSG-04, MSG-05, MSG-06, CSV-01]

must_haves:
  truths:
    - "Full Vitest suite passes with no failures after any fixes applied"
    - "generate-msg-btn, open-linkedin-btn, copy-msg-btn, mark-sent-btn, and export-csv-btn are all present in popup HTML"
    - "message-section is hidden by default and shown after a successful generateMessage call"
    - "open-linkedin-btn handler constructs a LinkedIn messaging URL using the candidate's profile path"
    - "export-csv-btn handler calls candidatesToCsv and downloadCsv from src/shared/csv.ts"
    - "messenger.ts TONE_MAP has entries for L1, L2, and L3 tiers"
  artifacts:
    - path: "linkedin-hhrh-screener/src/scorer/messenger.ts"
      provides: "generateOutreachMessage with TONE_MAP covering L1, L2, L3"
      contains: "TONE_MAP"
    - path: "linkedin-hhrh-screener/entrypoints/popup/index.ts"
      provides: "All output-layer button handlers wired and functional"
      contains: "generate-msg-btn"
    - path: "linkedin-hhrh-screener/entrypoints/popup/index.html"
      provides: "Message section and export section markup"
      contains: "message-textarea"
  key_links:
    - from: "entrypoints/popup/index.ts"
      to: "entrypoints/background.ts"
      via: "browser.runtime.sendMessage GENERATE_MESSAGE"
      pattern: "GENERATE_MESSAGE"
    - from: "entrypoints/popup/index.ts"
      to: "entrypoints/background.ts"
      via: "browser.runtime.sendMessage SAVE_MESSAGE"
      pattern: "SAVE_MESSAGE"
    - from: "entrypoints/popup/index.ts"
      to: "src/shared/csv.ts"
      via: "import candidatesToCsv, downloadCsv"
      pattern: "candidatesToCsv|downloadCsv"
---

<objective>
Run the full test suite after Wave 0 tests exist, diagnose any failures, and fix any implementation gaps found in the existing message generation and CSV export code.

Purpose: Phase 4 implementation was written before Wave 0 tests existed. The tests may reveal edge-case bugs (e.g., rejected tier guard, null candidate handling) or expose import issues in the popup wiring. This plan runs the suite, audits the popup handlers and messenger against requirements, and applies targeted fixes.

Output:
- Full vitest suite green (all tests pass)
- messenger.ts, popup/index.ts, popup/index.html, popup/style.css, and csv.ts corrected if any gaps are found
- No new features added — only fixes to existing implementation

Note: If the suite is already fully green and a code audit finds no gaps, the tasks collapse to read-and-confirm only. Do not add complexity for its own sake.
</objective>

<execution_context>
@/Users/danielphillips/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielphillips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-output-layer/04-RESEARCH.md
@.planning/phases/04-output-layer/04-01-SUMMARY.md

<interfaces>
<!-- Key types. Executor must read actual source files for full context before editing. -->

From linkedin-hhrh-screener/src/storage/schema.ts:
```typescript
export interface CandidateRecord {
  tier: 'L1' | 'L2' | 'L3' | 'rejected';
  outreachMessage: string;
  messageSentAt?: string;
  messageSentText?: string;
  contactAfter?: string;
}
```

From linkedin-hhrh-screener/src/shared/messages.ts — expected message types:
```typescript
// Expected shape (verify against actual file):
// { type: 'GENERATE_MESSAGE', candidateId: string }
// { type: 'SAVE_MESSAGE', candidateId: string, messageText: string }
```

From linkedin-hhrh-screener/src/scorer/messenger.ts — expected shape:
```typescript
// TONE_MAP must have 'L1', 'L2', 'L3' keys (not 'rejected')
// generateOutreachMessage(creds, profile, tier, matchedSkills, missingSkills, jdTitle)
// Returns: { message: string, error?: string }
```

Anti-patterns to avoid (from research):
- Do NOT move downloadCsv to background.ts — it requires DOM (popup-only)
- Do NOT auto-generate message on EVALUATE — keep explicit button
- Do NOT inject into LinkedIn compose window via scripting — URL approach only
- URL pattern for MSG-05: https://www.linkedin.com/messaging/compose/?recipient={encodedProfilePath}
- Cortex-only for v1 — do NOT add Anthropic fallback to messenger.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full suite and audit popup/messenger implementation</name>
  <files>
    linkedin-hhrh-screener/src/scorer/messenger.ts
    linkedin-hhrh-screener/entrypoints/popup/index.ts
    linkedin-hhrh-screener/entrypoints/popup/index.html
    linkedin-hhrh-screener/entrypoints/popup/style.css
    linkedin-hhrh-screener/src/shared/csv.ts
  </files>
  <action>
    Step 1 — Run the full suite and capture output:
    ```
    cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx vitest run
    ```

    Step 2 — Read the following files in full:
    - entrypoints/background.ts (lines 166-260, the message handlers and message listener wiring)
    - entrypoints/popup/index.ts (full file)
    - entrypoints/popup/index.html (full file)
    - src/scorer/messenger.ts (full file)
    - src/shared/messages.ts (full file)
    - src/shared/csv.ts (full file)

    Step 3 — For each requirement below, verify the implementation matches. If it does not, apply the minimal fix:

    MSG-01/02/03: messenger.ts TONE_MAP must have L1 (direct/enthusiastic), L2 (exploratory), L3 (future-opportunity with delayed-contact framing). Each entry must produce meaningfully different prompt instructions. If any tone is missing or identical across tiers, fix the prompt text to differentiate.

    MSG-04: popup/index.html must have a <textarea id="message-textarea"> that is editable (not readonly). popup/index.ts must read this textarea's value when mark-sent-btn is clicked.

    MSG-05: popup/index.ts open-linkedin-btn handler must construct:
    ```typescript
    const profilePath = new URL(currentProfileUrl).pathname;
    const msgUrl = `https://www.linkedin.com/messaging/compose/?recipient=${encodeURIComponent(profilePath)}`;
    await browser.tabs.create({ url: msgUrl });
    ```
    If currentProfileUrl is null/empty at click time, the button must do nothing or show an error — not throw.

    MSG-06: popup/index.ts mark-sent-btn must send { type: 'SAVE_MESSAGE', candidateId: currentCandidateId, messageText: textarea.value } to background and update the UI (e.g., show a "Marked as sent" confirmation).

    CSV-01: popup/index.ts export-csv-btn must call getAllCandidates(), pass results to candidatesToCsv(), then downloadCsv(csv, filename). Filename should be 'linkedin-candidates-{YYYY-MM-DD}.csv'.

    Step 4 — Fix any failures found in Step 1 test run. Common patterns:
    - Import errors: check that all named exports referenced in tests exist in the source files
    - Type mismatches: CandidateRecord fields must match schema.ts exactly
    - Missing message type guards: background.ts listener must handle GENERATE_MESSAGE and SAVE_MESSAGE cases

    Step 5 — After any edits, run the suite again to confirm it is green:
    ```
    cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx vitest run
    ```
  </action>
  <verify>
    <automated>cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx vitest run</automated>
  </verify>
  <done>Full vitest suite passes (0 failures). Implementation audit complete with any gaps corrected. SUMMARY documents which files were changed and what was fixed (or "no changes needed — implementation verified correct").</done>
</task>

<task type="auto">
  <name>Task 2: Verify popup message-section visibility and error state UX</name>
  <files>
    linkedin-hhrh-screener/entrypoints/popup/index.ts
    linkedin-hhrh-screener/entrypoints/popup/index.html
    linkedin-hhrh-screener/entrypoints/popup/style.css
  </files>
  <action>
    Read popup/index.html and popup/index.ts. Verify and fix these UX correctness points:

    1. message-section starts hidden (HTML `hidden` attribute or display:none via CSS). It must only become visible after a successful GENERATE_MESSAGE response. If it is visible on popup open before any evaluation, add the `hidden` attribute to the section in index.html.

    2. generate-msg-btn must be disabled/hidden until currentCandidateId is set (i.e., after EVALUATE completes in the current session). If there is no disabled guard, add one: set button.disabled = true in the initial state and enable it only after result.candidateId is received.

    3. Error handling for generate-msg-btn: if the GENERATE_MESSAGE response has result.error, display the error to the recruiter (e.g., in a #msg-error span or the existing #error-message element). Do not silently fail.

    4. copy-msg-btn handler: verify it calls navigator.clipboard.writeText(textarea.value). If missing or broken, implement it.

    5. After mark-sent-btn is clicked and SAVE_MESSAGE returns { saved: true }, show a brief confirmation to the recruiter (e.g., disable the button and change its text to 'Sent!' or show a small status message). This is a UX polish step — keep it minimal.

    Make only the changes identified above. Do not redesign or restructure the popup HTML. Do not add new UI sections.

    Run the suite one final time after any edits:
    ```
    cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx vitest run
    ```
  </action>
  <verify>
    <automated>cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx vitest run</automated>
  </verify>
  <done>Suite still passes after popup UX fixes. All five UX points verified (hidden message-section, disabled generate button, error display, clipboard copy, mark-sent confirmation).</done>
</task>

</tasks>

<verification>
Final targeted suite run:
```
cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx vitest run tests/messenger.test.ts tests/background.test.ts tests/csv.test.ts
```

All tests pass. No TypeScript compilation errors.
</verification>

<success_criteria>
- Full vitest suite green (npx vitest run produces 0 failures)
- messenger.ts TONE_MAP covers L1, L2, L3 with distinct prompt instructions
- popup/index.ts correctly wires all five output-layer buttons
- message-section hidden by default, visible after GENERATE_MESSAGE success
- generate-msg-btn disabled until candidateId available
- Error states surfaced to recruiter (not silently swallowed)
- CSV export button correctly generates and downloads file
- SUMMARY documents what was changed (or confirmed correct without changes)
</success_criteria>

<output>
After completion, create `.planning/phases/04-output-layer/04-02-SUMMARY.md`
</output>
