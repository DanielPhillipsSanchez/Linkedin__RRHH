---
phase: 05-scheduling-and-notifications
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified: []
autonomous: false
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04]

must_haves:
  truths:
    - "Evaluating an L3 candidate in Chrome creates a visible alarm entry in the Chrome extensions page"
    - "The extension icon badge shows the correct overdue L3 count after the contact window opens"
    - "A system notification appears when the alarm fires (verified by triggering via DevTools)"
    - "The popup shows the L3 Follow-ups Due section with overdue candidates listed"
    - "The badge decrements (or clears) after the recruiter clicks Mark as Sent for the overdue candidate"
  artifacts:
    - path: "linkedin-hhrh-screener/entrypoints/background.ts"
      provides: "All SCHED logic implemented and verified end-to-end"
    - path: "linkedin-hhrh-screener/entrypoints/popup/index.html"
      provides: "overdue-section rendered in Chrome popup"
  key_links:
    - from: "Chrome extension icon badge"
      to: "refreshBadge() in background.ts"
      via: "overdue L3 count query"
      pattern: "badge visible after L3 evaluation"
    - from: "Chrome OS notification"
      to: "browser.alarms.onAlarm listener"
      via: "alarm fires → notifications.create()"
      pattern: "notification appears after alarm trigger"
---

<objective>
Human verification checkpoint: build the extension and confirm all four SCHED requirements work in a live Chrome environment.

Purpose: Unit tests cover alarm creation, notification logic, and badge refresh in isolation. This checkpoint confirms the assembled behavior in real Chrome — system notifications, badge rendering, and popup overdue section.
Output: Recruiter confirms SCHED-01 through SCHED-04 work end-to-end in Chrome. Phase 5 complete.
</objective>

<execution_context>
@/Users/danielphillips/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielphillips/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-scheduling-and-notifications/05-01-SUMMARY.md
@.planning/phases/05-scheduling-and-notifications/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build extension and verify full test suite</name>
  <files></files>
  <action>
    Run the full test suite to confirm all SCHED tests pass and no regressions exist:
    `cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx vitest run`

    Then build the extension for Chrome:
    `cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx wxt build`

    After build succeeds, verify the icon asset exists in the output (required for notifications.create):
    `ls /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener/.output/chrome-mv3/icon/128.png`

    If the icon path is different, update `browser.runtime.getURL('/icon/128.png')` in background.ts to use the correct path found in `.output/chrome-mv3/`. Rebuild if changed.

    Report build output location to the recruiter.
  </action>
  <verify>
    <automated>cd /Users/danielphillips/Documents/Linkedin__RRHH/linkedin-hhrh-screener && npx vitest run && npx wxt build</automated>
  </verify>
  <done>Full test suite green, wxt build completes without errors, .output/chrome-mv3/ exists with icon asset confirmed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 scheduling and notification features:
    - SCHED-01: Evaluating an L3 candidate creates a browser.alarms entry scheduled 7 days out
    - SCHED-02: When the alarm fires, a system OS notification appears naming the candidate
    - SCHED-03: The extension icon badge shows the count of overdue uncontacted L3 candidates
    - SCHED-04: The popup displays an "L3 Follow-ups Due" section listing overdue candidates
  </what-built>
  <how-to-verify>
    Load the extension:
    1. Open Chrome → chrome://extensions → Enable Developer mode → Load unpacked → select `.output/chrome-mv3/`

    Test SCHED-01 (alarm creation):
    2. Open a LinkedIn profile page
    3. Open the extension popup, click Evaluate
    4. If the candidate scores L3 (60-70%): proceed. If not, adjust the active JD to include more mandatory skills the candidate is missing until you get an L3 result
    5. After evaluation, go to chrome://extensions → find "HHRH Screener" → click "Service worker" (inspect background) → in DevTools console run: `chrome.alarms.getAll(console.log)`
    6. Confirm an alarm entry named `l3-followup-{uuid}` appears with a scheduledTime approximately 7 days from now

    Test SCHED-04 (popup overdue list):
    7. Open the popup — confirm the "L3 Follow-ups Due" section appears
    8. If the candidate's contactAfter is in the future (7 days out), the section shows "No L3 candidates awaiting contact" — this is correct for a freshly evaluated candidate

    Test SCHED-02 and SCHED-03 (alarm fire and notification — requires manual trigger):
    9. In the background service worker DevTools console, run:
       `chrome.alarms.create('l3-followup-test', { when: Date.now() + 1000 })`
       Then save a test candidate to storage so the alarm handler can look it up. OR:
       Use the actual alarm: in background DevTools console run `chrome.alarms.getAll(console.log)` to get the alarm name, then manually fire it:
       `chrome.alarms.clear('l3-followup-{uuid}')` then re-create with `when: Date.now() + 2000`
    10. Wait 2 seconds — a system OS notification should appear saying "L3 Follow-up Ready" and naming the candidate
    11. Check the extension icon badge — it should show a count (may be 0 if contactAfter is in the future for the evaluated candidate; the badge only counts candidates whose contactAfter has already passed)

    Test SCHED-03 badge decrement:
    12. Open popup → find the evaluated candidate in Recent Candidates → click Mark as Sent
    13. Confirm badge clears or decrements (if badge was showing a count)

    Test SCHED-04 empty state:
    14. Open popup with no overdue L3 candidates — confirm "No L3 candidates awaiting contact" is displayed in the L3 Follow-ups Due section
  </how-to-verify>
  <resume-signal>Type "approved" if all verifiable steps pass. Describe any issues found — include which step failed and what was observed instead of expected behavior.</resume-signal>
</task>

</tasks>

<verification>
All four SCHED requirements confirmed by human recruiter in Chrome:
- SCHED-01: Alarm entry visible in chrome.alarms.getAll() after L3 evaluation
- SCHED-02: System notification appears when alarm fires
- SCHED-03: Badge reflects overdue L3 count; decrements after Mark as Sent
- SCHED-04: Popup overdue section renders correctly (list when overdue, empty state when none)
</verification>

<success_criteria>
- Build produces .output/chrome-mv3/ with no errors
- Human recruiter approves all four SCHED requirements in Chrome
- Phase 5 complete — ready for Phase 6 Safari Packaging
</success_criteria>

<output>
After completion, create `.planning/phases/05-scheduling-and-notifications/05-03-SUMMARY.md`
</output>
